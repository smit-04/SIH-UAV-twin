# Digital Twin Core Handoff Interface

The Digital Twin Core receives validated normalized telemetry matching the contract defined in `docs/architecture/PHASE_2_CONTRACTS.md`.

No CAN decoding, binary unpack, or transport logic is required inside the Digital Twin Core.
